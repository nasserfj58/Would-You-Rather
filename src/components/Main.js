import React from 'react';
import { Alert } from 'react-bootstrap';
import { NavigationBar } from './NavigationBar';

const Main = () => {
    return (

        <div>
            <NavigationBar />
        </div>

    );
}

export default Main;